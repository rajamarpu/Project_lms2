{
  "name": "lms-root",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix course-compass-main run dev",
    "backend": "npm --prefix backend run dev",
    "admin": "npm --prefix admin run dev",
    "dev:all": "concurrently \"npm run backend\" \"npm run dev\" \"npm run admin\"",
    "dev:backend": "npm run backend",
    "dev:frontend": "npm run dev",
    "dev:admin": "npm run admin",
    "install:all": "npm install && cd backend && npm install && cd ../course-compass-main && npm install && cd ../admin && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
